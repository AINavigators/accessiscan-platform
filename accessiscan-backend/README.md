# accessiscan-backend
A FastAPI-based backend service that manages accessibility audits, processes user requests, and coordinates AI-driven analysis. The system handles data storage, authentication, classification logic, DSAPT mapping, and report generation.
